---
kind: dango.exchange
version: 1
status: needs_orchestrator_review
skill: elevation_lookup
task_type: plan_polish
selected: false
---

## Memo
- The provided input is suitable only for this skill's preprocessing scope if it contains messy JSON with site coordinates and groundwater level measurements.
- No execution should occur during polish: do not parse records yet, do not call elevation lookup yet, and do not emit enriched/model-ready artifacts yet.
- The assigned task as written is primarily about downstream modeling and CSV prediction export; that is outside this skill's end responsibility.
- This skill can contribute an upstream enrichment step only: normalize observations, identify usable coordinates, and prepare elevation-enriched structured handoff data for a separate modeling skill.

## Reasoning
Feasible as a preprocessing handoff stage, not as the final selected skill for the full task. The dataset clearly contains inconsistent coordinate and groundwater-depth fields that can be normalized, and usable coordinates can later be passed through the deterministic in-skill elevation API shim. However, building a predictive model for arbitrary Honshu locations and saving prediction CSV outputs is downstream modeling work, so this skill should not be selected as the sole executor.

## Handoff
Revised execution approach for orchestrator review:

1. Accept messy JSON containing site coordinates and groundwater water-level measurements, including nested batches and inconsistent field names.
2. In execution, parse and normalize all observation records into a compact canonical schema:
   - site identifier/name
   - latitude
   - longitude
   - groundwater level or depth value
   - units / confidence / estimate flags where recoverable
   - source path within the messy JSON
3. For every usable coordinate, call this skill's deterministic in-skill elevation API shim to append site elevation metadata.
4. Produce a compact structured JSON handoff for downstream modeling, not final predictions:
   - normalized observations
   - elevation-enriched site records
   - rejected/unusable records with reasons
   - summary counts
5. Route the enriched JSON to a separate modeling skill that will:
   - train or fit the groundwater-level prediction workflow
   - generate arbitrary-location predictions
   - save prediction values as CSV for later analysis
6. Do not plan any PDF or report formatting work.

Selection note: if the orchestrator needs only modeling, CSV generation, PDF rendering, or report formatting, this skill should not be selected.