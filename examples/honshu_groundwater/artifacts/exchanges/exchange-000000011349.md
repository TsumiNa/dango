---
kind: dango-exchange
status: ready-for-orchestrator-review
resources: []
---

## Memo
- Feasible and in scope for this skill.
- Required input is the **exact messy JSON payload**, containing site coordinates and groundwater level measurements.
- No PDF, reporting, or modeling work should be added here.
- Preferred execution is the canonical single-step enrichment path; no custom parsing layer should be introduced unless a required transformation is explicitly outside what the script already emits.

## Reasoning
The current draft is not adequate: it incorrectly frames the task as report generation. The actual task is structured groundwater observation normalization plus elevation enrichment. That matches this skill. Feasibility is good because the execution path is already defined: `scripts/enrich.py` accepts the raw messy JSON text, parses supported schema variants, and calls the deterministic in-skill elevation API shim for every usable coordinate. The correct downstream deliverable is compact structured JSON for modeling, not a narrative report.

## Handoff
Revised execution approach for orchestrator review:

1. **Input requirement**
   - Use the **exact messy JSON payload from the request** as the sole input source.
   - The input must contain site coordinates and groundwater level measurements.
   - It may include both top-level `records` entries and nested content under `regional_batches`; object-wrapped and array batch forms should be passed through unchanged as raw input text for the script to parse.

2. **Execution method**
   - Run `scripts/enrich.py`.
   - The script will parse observation records from the messy JSON, normalize usable sites, and call the deterministic in-skill elevation API shim for every usable coordinate.
   - Do **not** add ad-hoc parsing glue unless the orchestrator has confirmed a required output transformation that `enrich.py` cannot produce.

3. **Canonical command**
   ```sh
   uv --directory /Users/liuchang/projects/dango/examples/honshu_groundwater/elevation_lookup run python scripts/enrich.py <<'DANGO_JSON'
   { "observations_json": <messy JSON as a JSON string> }
   DANGO_JSON
   ```

4. **Expected output / handoff**
   - The handoff will be a **JSON object suitable for downstream modeling**.
   - It should contain normalized observation rows with fields including coordinates, `water_level_m_bgl`, and `elevation_m` where coordinates are usable, along with related metadata emitted by the script.
   - In actual execution, paste the script stdout JSON into a fenced `json` block as the Handoff body for downstream extraction.

5. **Scope note**
   - This skill should **not** be selected if the task is only about modeling, PDF rendering, or report formatting.
   - For this request, the skill **is** the right fit because the work is messy groundwater JSON parsing/flattening plus elevation enrichment.