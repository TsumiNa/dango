---
resources: []
---

# Memo

In scope and ready for canonical execution.

The current planner draft is too generic. This task is specifically about **messy JSON groundwater observations** and should use the skill’s built-in enrichment path, not a custom analysis/report workflow.

No tool calls, file writes, or script execution are needed during this feasibility review.

# Reasoning

Feasible with the skill’s standard one-step execution.

The task matches this skill because it requires:
- messy JSON groundwater observation parsing;
- normalization of usable site observations from top-level records and nested `regional_batches`;
- preservation of site identity and uncertainty/ambiguity metadata where available;
- elevation enrichment for usable coordinates;
- compact JSON output for downstream modeling.

The revised plan should rely on `scripts/enrich.py` exactly once, with the provided user input treated as the raw messy JSON text. No ad-hoc parsing glue should be added unless the requested transformation exceeds what the canonical script supports.

# Handoff

Use this skill.

## Revised execution approach for orchestrator review

**Input**
- The input must be the provided **messy JSON text** containing groundwater site coordinates and groundwater level measurements.
- Treat the user-supplied payload as raw observation JSON, including both top-level records and nested `regional_batches` entries.

**Execution**
- Run the canonical parser/enricher exactly once:
  - `scripts/enrich.py`
- Pass the messy JSON as a **raw JSON string on stdin**.
- The script will parse observations, normalize usable records, and call the deterministic in-skill elevation API shim for every usable coordinate.
- Do **not** add ad-hoc parsing glue beyond what the canonical script already supports.

Canonical command:

```sh
uv --directory /Users/liuchang/projects/dango/examples/honshu_groundwater/elevation_lookup run python scripts/enrich.py <<'DANGO_JSON'
{ "observations_json": "<messy JSON as a JSON string>" }
DANGO_JSON
```

**Normalization expectations**
- Preserve for each usable observation:
  - `latitude`
  - `longitude`
  - `elevation_m`
  - `water_level_m_bgl`
- Also preserve, where available:
  - stable site identifier or name
  - estimated/direct or confidence metadata
  - ambiguity notes such as `approx BGL` or casing-reference caveats
  - relevant source text/metadata fields already emitted by the script

**Exclusions**
- If a record cannot be normalized into both usable coordinates and groundwater depth, exclude it from the structured observations output.
- Summarize exclusions and parsing assumptions in the execution handoff narrative.

**Output / handoff**
- The script’s stdout is expected to be a **compact structured JSON object suitable for downstream modeling**.
- In the execution exchange, paste that stdout JSON into the Handoff section inside a fenced `json` block.
- No artifact files are required for the canonical run, so `resources` may remain empty unless output persistence is explicitly added later.

**Not the right skill if**
- The task is only downstream modeling, PDF rendering, or report formatting. In those cases, this skill should not be selected.