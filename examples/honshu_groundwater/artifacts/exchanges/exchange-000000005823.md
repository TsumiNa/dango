---
resources: []
---

# Memo

Ready and in scope for this skill.

Key correction to the draft: this is not a generic “analyze data and generate a report” task. The input must be **messy JSON containing site coordinates and groundwater level measurements**, and the execution should use the skill’s existing parser/enricher rather than bespoke data-cleaning code.

No tools should be called during polish. No files need to be written for the canonical run.

# Reasoning

Feasible as a single enrichment pass.

The assigned task matches this skill because it requires:
- parsing messy groundwater observation JSON;
- handling mixed coordinate and water-level field variants;
- enriching usable coordinates with elevation via the deterministic in-skill lookup shim;
- returning a compact structured JSON handoff suitable for downstream modeling.

The planner draft should be revised to use the canonical execution path: run `scripts/enrich.py` once with the raw messy JSON on stdin. That script is the intended mechanism for parsing both top-level observations and nested structures, normalizing records, and performing elevation enrichment. A separate reporting or PDF-oriented skill is not needed here.

# Handoff

Use this skill.

## Revised execution approach for orchestrator review

**Input requirement**
- The input must be the provided **messy JSON text** containing groundwater survey observations, including site coordinates and groundwater level measurements.
- The messy JSON may include observations at the top level and inside nested `regional_batches` entries.

**Execution**
- Run `scripts/enrich.py`, passing the messy JSON as a raw JSON string on stdin.
- The script will parse observations, normalize usable records, and call the deterministic in-skill elevation API shim for every usable coordinate.
- Do **not** add ad-hoc parsing glue unless the task is explicitly expanded beyond what `enrich.py` already supports.

Canonical command:

```sh
uv --directory /Users/liuchang/projects/dango/examples/honshu_groundwater/elevation_lookup run python scripts/enrich.py <<'DANGO_JSON'
{ "observations_json": "<messy JSON as a JSON string>" }
DANGO_JSON
```

**Expected handoff**
- The handoff will be a **JSON object suitable for downstream modeling**, emitted by the script to stdout.
- In the execution exchange, paste that stdout inside a ```json fenced block in the Handoff section.
- Canonical run does not need to write artifact files; `resources` can remain empty unless the orchestrator explicitly persists outputs elsewhere.

**Notes for orchestrator**
- This skill is appropriate because the task is about messy groundwater observation normalization plus elevation enrichment.
- If the task were only about downstream modeling, PDF rendering, or report formatting, this skill should **not** be selected.